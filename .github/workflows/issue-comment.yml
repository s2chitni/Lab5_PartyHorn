name: Gif Comment
on:
  issues:
    types: [opened]

jobs:
  comment:
    if: github.event_name == 'issues'
    runs-on: ubuntu-latest
    steps: 
      - name: Add Gif comment
        uses: s2chitni@Lab5-PartyHorn
        id: GifComment
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GIPHY_TOKEN: ${{ secrets.GIPHY_TOKEN }} 
          comment_type: 'gif'
    
